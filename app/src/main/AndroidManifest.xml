<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pluscomputers.mosque">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/mosque"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/mosque"
        android:supportsRtl="true"
        android:theme="@style/HiddenTitleTheme">
        <activity android:name=".LoginActivity" />
        <activity android:name=".AboutActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NavigationActivity"></activity>
        <activity android:name=".DonacionetActivity"></activity>
        <activity android:name=".Islaminewil_Activity"></activity>
    </application>

</manifest>